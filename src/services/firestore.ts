/**
 * @deprecated Use '../api' instead. This file is kept for backward compatibility.
 * Import from '../api' for new code:
 * - import { createAccount, updateAccount, deleteAccount, getAllAccounts } from '../api';
 * - import { createEntry, updateEntry, deleteEntry, getEntriesByAccount } from '../api';
 */
export {
  createAccount,
  updateAccount,
  deleteAccount,
  getAllAccounts as getAccounts,
  createEntry,
  updateEntry,
  deleteEntry,
  getEntriesByAccount,
} from '../api';
