/**
 * @deprecated Use '../config' instead. This file is kept for backward compatibility.
 * Import from '../config' for new code: import { app, db } from '../config';
 */
export { app, db } from '../config';
